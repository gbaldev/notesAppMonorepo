import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexShrink: 1,
    alignItems: 'center',
    paddingBottom: 10,
    // backgroundColor: 'red',
  },
  flatlist: {width: '100%', flexGrow: 1},
});
